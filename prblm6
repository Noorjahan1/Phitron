#include <bits/stdc++.h>

using namespace std;


int main()
{
    string s;
    stack<char>st;


    cin>>s;
    for(char c: s){
        st.push(c);
    }

    while(!st.empty()){
        cout<<st.top();
        st.pop();
    }

    return 0;
}

